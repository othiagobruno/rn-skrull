export interface ITheme {
  primary: string;
  backgroundInput: string;
  background: string;
  text: string;
  label: string;
  appBar: AppBar;
}

interface AppBar {
  color: string;
  background: string;
}
